---
description: lowcode-wrapper 项目约定与 FDW sidecar 架构
alwaysApply: true
---

# lowcode-wrapper

- Go 1.21+ HTTP 服务，`net/http` + JSON，**无 gRPC/protobuf**
- **单 Meta DB**：`DATABASE_URL` 存 wrapper_* 元数据；`make migrate` 应用 `scripts/migrations/*.up.sql`
- **凭据**：`WRAPPER_MASTER_KEY`（32-byte base64）+ AES-256-GCM；`credentialRef` 引用 UUID，spec 不落明文
- **Admin API**：`/api/credentials`、`/api/servers`、`/api/tables`、`/api/functions`
- **数据 API**：PostgREST 风格 `/v1/{schema}/{table}`、`POST /v1/rpc/{fn}`
- **驱动**：`internal/driver` 注册表；`http`/`postgres`/`mysql`/`file`/`mongo`/`redis`/`s3`；`notion`/`firebase` 委托 `httpdriver`（`internal/driver/httpwrap`）
- HTTP auth：`internal/auth/httpauth.go`（NONE / BASIC / API_KEY / BEARER / CLIENT_CREDENTIALS / UNIVERSAL）
- 列映射：本地 `wrapper_column.name` → 远程 `remote_name`；filter/body 经 `postgrest.MapRowToRemote` / `MapFilters`
- 日志：stdlib `log/slog`，`LOG_LEVEL` / `LOG_FORMAT`（text|json）；HTTP 访问日志在 `internal/api/logging.go`
- 测试：`make test`；集成测试需 `DATABASE_URL`（`internal/integration/`）
- 小 diff，匹配现有 handler / store / driver 布局；未要求不 git commit
